-r requirements.txt
# Development dependencies are now managed in pyproject.toml
# Install with: pip install -e ".[dev]"
# Or install specific groups:
#   pip install -e ".[test]"  # Testing only
#   pip install -e ".[lint]"  # Linting only
#   pip install -e ".[dev]"    # All dev dependencies

# For backward compatibility, dependencies are listed below:
# (Consider using pyproject.toml instead)
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-mock>=3.11.0,<4.0.0
black>=23.0.0,<25.0.0
ruff>=0.1.0,<1.0.0
mypy>=1.5.0,<2.0.0

